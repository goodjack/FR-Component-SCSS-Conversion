import React from 'react';
import MarkdownElement from '../../MarkdownElement';
import usageText from './guidelines.md';

const Usage = () => (
  <MarkdownElement text={usageText} />
);

export default Usage;
